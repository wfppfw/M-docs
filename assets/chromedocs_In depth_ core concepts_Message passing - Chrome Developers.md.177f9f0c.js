import{_ as e,o as t,c as n,a as s}from"./app.8d96fb22.js";const u=JSON.parse('{"title":"Message passing","description":"","frontmatter":{},"headers":[{"level":2,"title":"# Simple one-time requests","slug":"simple-one-time-requests","link":"#simple-one-time-requests","children":[]},{"level":2,"title":"# Long-lived connections","slug":"long-lived-connections","link":"#long-lived-connections","children":[{"level":3,"title":"# Port lifetime","slug":"port-lifetime","link":"#port-lifetime","children":[]}]},{"level":2,"title":"# Cross-extension messaging","slug":"cross-extension-messaging","link":"#cross-extension-messaging","children":[]},{"level":2,"title":"# Sending messages from web pages","slug":"sending-messages-from-web-pages","link":"#sending-messages-from-web-pages","children":[]},{"level":2,"title":"# Native messaging","slug":"native-messaging","link":"#native-messaging","children":[]},{"level":2,"title":"# Security considerations","slug":"security-considerations","link":"#security-considerations","children":[{"level":3,"title":"# Content scripts are less trustworthy","slug":"content-scripts-are-less-trustworthy","link":"#content-scripts-are-less-trustworthy","children":[]},{"level":3,"title":"# Cross-site scripting","slug":"cross-site-scripting","link":"#cross-site-scripting","children":[]}]},{"level":2,"title":"# Examples","slug":"examples","link":"#examples","children":[]}],"relativePath":"chromedocs/In depth_ core concepts/Message passing - Chrome Developers.md","lastUpdated":1679926139000}'),r={name:"chromedocs/In depth_ core concepts/Message passing - Chrome Developers.md"},o=s(`<ul><li><a href="https://developer.chrome.com/docs/extensions/mv3/messaging/#simple" target="_blank" rel="noreferrer">Simple one-time requests</a></li><li><a href="https://developer.chrome.com/docs/extensions/mv3/messaging/#connect" target="_blank" rel="noreferrer">Long-lived connections</a><ul><li><a href="https://developer.chrome.com/docs/extensions/mv3/messaging/#port-lifetime" target="_blank" rel="noreferrer">Port lifetime</a></li></ul></li><li><a href="https://developer.chrome.com/docs/extensions/mv3/messaging/#external" target="_blank" rel="noreferrer">Cross-extension messaging</a></li><li><a href="https://developer.chrome.com/docs/extensions/mv3/messaging/#external-webpage" target="_blank" rel="noreferrer">Sending messages from web pages</a></li><li><a href="https://developer.chrome.com/docs/extensions/mv3/messaging/#native-messaging" target="_blank" rel="noreferrer">Native messaging</a></li><li><a href="https://developer.chrome.com/docs/extensions/mv3/messaging/#security-considerations" target="_blank" rel="noreferrer">Security considerations</a><ul><li><a href="https://developer.chrome.com/docs/extensions/mv3/messaging/#content-scripts-are-less-trustworthy" target="_blank" rel="noreferrer">Content scripts are less trustworthy</a></li><li><a href="https://developer.chrome.com/docs/extensions/mv3/messaging/#cross-site-scripting" target="_blank" rel="noreferrer">Cross-site scripting</a></li></ul></li><li><a href="https://developer.chrome.com/docs/extensions/mv3/messaging/#examples" target="_blank" rel="noreferrer">Examples</a></li></ul><h1 id="message-passing" tabindex="-1">Message passing <a class="header-anchor" href="#message-passing" aria-hidden="true">#</a></h1><p>Published on Tuesday, September 18, 2012 \u2022 Updated on Friday, November 11, 2022</p><ul><li><a href="https://developer.chrome.com/docs/extensions/mv3/messaging/#simple" target="_blank" rel="noreferrer">Simple one-time requests</a></li><li><a href="https://developer.chrome.com/docs/extensions/mv3/messaging/#connect" target="_blank" rel="noreferrer">Long-lived connections</a><ul><li><a href="https://developer.chrome.com/docs/extensions/mv3/messaging/#port-lifetime" target="_blank" rel="noreferrer">Port lifetime</a></li></ul></li><li><a href="https://developer.chrome.com/docs/extensions/mv3/messaging/#external" target="_blank" rel="noreferrer">Cross-extension messaging</a></li><li><a href="https://developer.chrome.com/docs/extensions/mv3/messaging/#external-webpage" target="_blank" rel="noreferrer">Sending messages from web pages</a></li><li><a href="https://developer.chrome.com/docs/extensions/mv3/messaging/#native-messaging" target="_blank" rel="noreferrer">Native messaging</a></li><li><a href="https://developer.chrome.com/docs/extensions/mv3/messaging/#security-considerations" target="_blank" rel="noreferrer">Security considerations</a><ul><li><a href="https://developer.chrome.com/docs/extensions/mv3/messaging/#content-scripts-are-less-trustworthy" target="_blank" rel="noreferrer">Content scripts are less trustworthy</a></li><li><a href="https://developer.chrome.com/docs/extensions/mv3/messaging/#cross-site-scripting" target="_blank" rel="noreferrer">Cross-site scripting</a></li></ul></li><li><a href="https://developer.chrome.com/docs/extensions/mv3/messaging/#examples" target="_blank" rel="noreferrer">Examples</a></li></ul><p>Since content scripts run in the context of a web page and not the extension, they often need some way of communicating with the rest of the extension. For example, an RSS reader extension might use content scripts to detect the presence of an RSS feed on a page, then notify the background page in order to display a page action icon for that page.</p><p>Communication between extensions and their content scripts works by using message passing. Either side can listen for messages sent from the other end, and respond on the same channel. A message can contain any valid JSON object (null, boolean, number, string, array, or object). There is a simple API for <a href="https://developer.chrome.com/docs/extensions/mv3/messaging/#simple" target="_blank" rel="noreferrer">one-time requests</a> and a more complex API for <a href="https://developer.chrome.com/docs/extensions/mv3/messaging/#connect" target="_blank" rel="noreferrer">long-lived connections</a> to exchange multiple messages within shared context. It is also possible to send a message to another extension if you know its ID. That is covered in the <a href="https://developer.chrome.com/docs/extensions/mv3/messaging/#external" target="_blank" rel="noreferrer">cross-extension messages</a> section.</p><h2 id="simple-one-time-requests" tabindex="-1"><a href="https://developer.chrome.com/docs/extensions/mv3/messaging/#simple" target="_blank" rel="noreferrer">#</a> Simple one-time requests <a class="header-anchor" href="#simple-one-time-requests" aria-hidden="true">#</a></h2><p>If you only need to send a single message to another part of your extension (and optionally get a response back), use the simplified <a href="https://developer.chrome.com/docs/extensions/reference/runtime#method-sendMessage" target="_blank" rel="noreferrer">runtime.sendMessage()</a> or <a href="https://developer.chrome.com/docs/extensions/reference/tabs#method-sendMessage" target="_blank" rel="noreferrer">tabs.sendMessage()</a>. This lets you send a one-time JSON-serializable message from a content script to the extension, or vice versa. To handle the response, use the returned Promise. (For backward compatibility, you can still pass a callback as the last argument.)</p><p>Sending a request from a content script looks like this:</p><pre><code>await response = chrome.runtime.sendMessage({greeting: &quot;hello&quot;});console.log(response.farewell);
</code></pre><p>Sending a request from the extension to a content script is similar, except that you need to specify which tab to send it to. This example demonstrates sending a message to the content script in the selected tab.</p><pre><code>chrome.tabs.query({active: true, lastFocusedWindow: true}, function(tabs) {  await response = chrome.tabs.sendMessage(tabs[0].id, {greeting: &quot;hello&quot;});  console.log(response.farewell);});
</code></pre><p>On the receiving end, you need to set up an <a href="https://developer.chrome.com/docs/extensions/reference/runtime#event-onMessage" target="_blank" rel="noreferrer">runtime.onMessage</a> event listener to handle the message. This looks the same from a content script or extension page.</p><pre><code>chrome.runtime.onMessage.addListener(  function(request, sender, sendResponse) {    console.log(sender.tab ?                &quot;from a content script:&quot; + sender.tab.url :                &quot;from the extension&quot;);    if (request.greeting === &quot;hello&quot;)      sendResponse({farewell: &quot;goodbye&quot;});  });
</code></pre><p>In the above example, <code>sendResponse()</code> was called synchronously. If you want to asynchronously use <code>sendResponse()</code>, add <code>return true;</code> to the <code>onMessage</code> event handler.</p><p>If multiple pages are listening for <code>onMessage</code> events, only the first to call <code>sendResponse()</code> for a particular event will succeed in sending the response. All other responses to that event will be ignored.</p><p>For new extensions you should prefer promises over callbacks. If you&#39;re using callbacks, the <code>sendResponse()</code> callback is only valid if used synchronously, or if the event handler returns <code>true</code> to indicate that it will respond asynchronously. The <code>sendMessage()</code> function&#39;s callback will be invoked automatically if no handlers return true or if the <code>sendResponse()</code> callback is garbage-collected.</p><h2 id="long-lived-connections" tabindex="-1"><a href="https://developer.chrome.com/docs/extensions/mv3/messaging/#connect" target="_blank" rel="noreferrer">#</a> Long-lived connections <a class="header-anchor" href="#long-lived-connections" aria-hidden="true">#</a></h2><p>Sometimes it&#39;s useful to have a conversation that lasts longer than a single request and response. In this case, you can open a long-lived channel from your content script to an extension page or vice versa using <a href="https://developer.chrome.com/docs/extensions/reference/runtime#method-connect" target="_blank" rel="noreferrer">runtime.connect</a> or <a href="https://developer.chrome.com/docs/extensions/reference/tabs#method-connect" target="_blank" rel="noreferrer">tabs.connect</a>, respectively. The channel can optionally have a name, allowing you to distinguish between different types of connections.</p><p>One use case might be an automatic form filling extension. The content script could open a channel to the extension page for a particular login, and send a message to the extension for each input element on the page to request the form data to fill in. The shared connection allows the extension to keep shared state linking the messages coming from the content script.</p><p>When establishing a connection, each end is given a <a href="https://developer.chrome.com/docs/extensions/reference/runtime#type-Port" target="_blank" rel="noreferrer">runtime.Port</a> object which is used for sending and receiving messages through that connection.</p><p>Here is how you open a channel from a content script, and send and listen for messages:</p><pre><code>var port = chrome.runtime.connect({name: &quot;knockknock&quot;});port.postMessage({joke: &quot;Knock knock&quot;});port.onMessage.addListener(function(msg) {  if (msg.question === &quot;Who&#39;s there?&quot;)    port.postMessage({answer: &quot;Madame&quot;});  else if (msg.question === &quot;Madame who?&quot;)    port.postMessage({answer: &quot;Madame... Bovary&quot;});});
</code></pre><p>Sending a request from the extension to a content script is similar, except that you need to specify which tab to connect to. Simply replace the call to connect in the above example with <a href="https://developer.chrome.com/docs/extensions/reference/tabs#method-connect" target="_blank" rel="noreferrer">tabs.connect()</a>.</p><p>To handle incoming connections, you need to set up a <a href="https://developer.chrome.com/docs/extensions/reference/runtime#event-onConnect" target="_blank" rel="noreferrer">runtime.onConnect()</a> event listener. This looks the same from a content script or an extension page. When another part of your extension calls <code>connect()</code>, this event is fired, along with the <a href="https://developer.chrome.com/docs/extensions/reference/runtime#type-Port" target="_blank" rel="noreferrer">runtime.Port</a> object you can use to send and receive messages through the connection. Here&#39;s what it looks like to respond to incoming connections:</p><pre><code>chrome.runtime.onConnect.addListener(function(port) {  console.assert(port.name === &quot;knockknock&quot;);  port.onMessage.addListener(function(msg) {    if (msg.joke === &quot;Knock knock&quot;)      port.postMessage({question: &quot;Who&#39;s there?&quot;});    else if (msg.answer === &quot;Madame&quot;)      port.postMessage({question: &quot;Madame who?&quot;});    else if (msg.answer === &quot;Madame... Bovary&quot;)      port.postMessage({question: &quot;I don&#39;t get it.&quot;});  });});
</code></pre><h3 id="port-lifetime" tabindex="-1"><a href="https://developer.chrome.com/docs/extensions/mv3/messaging/#port-lifetime" target="_blank" rel="noreferrer">#</a> Port lifetime <a class="header-anchor" href="#port-lifetime" aria-hidden="true">#</a></h3><p>Ports are designed as a two-way communication method between different parts of the extension, where a (top-level) frame is viewed as the smallest part. Upon calling <a href="https://developer.chrome.com/docs/extensions/reference/tabs#method-connect" target="_blank" rel="noreferrer">tabs.connect()</a>, <a href="https://developer.chrome.com/docs/extensions/reference/runtime#method-connect" target="_blank" rel="noreferrer">runtime.connect()</a> or <a href="https://developer.chrome.com/docs/extensions/reference/runtime#method-connectNative" target="_blank" rel="noreferrer">runtime.connectNative()</a>, a <a href="https://developer.chrome.com/docs/extensions/reference/runtime#type-Port" target="_blank" rel="noreferrer">Port</a> is created. This port can immediately be used for sending messages to the other end via <a href="https://developer.chrome.com/docs/extensions/reference/runtime#property-Port-postMessage" target="_blank" rel="noreferrer">postMessage()</a>.</p><p>If there are multiple frames in a tab, calling <a href="https://developer.chrome.com/docs/extensions/reference/tabs#method-connect" target="_blank" rel="noreferrer">tabs.connect()</a> results in multiple invocations of the <a href="https://developer.chrome.com/docs/extensions/reference/runtime#event-onConnect" target="_blank" rel="noreferrer">runtime.onConnect</a> event, once for each frame in the tab. Similarly, if <a href="https://developer.chrome.com/docs/extensions/reference/runtime#method-connect" target="_blank" rel="noreferrer">runtime.connect()</a> is called, then the onConnect event may be fired multiple times, once for every frame in the extension process.</p><p>You may want to find out when a connection is closed, for example if you are maintaining separate state for each open port. For this listen to the <a href="https://developer.chrome.com/docs/extensions/reference/runtime#property-Port-onDisconnect" target="_blank" rel="noreferrer">runtime.Port.onDisconnect</a> event. This event is fired when there are no valid ports at the other side of the channel. This happens in the following situations:</p><ul><li>There are no listeners for <a href="https://developer.chrome.com/docs/extensions/reference/runtime#event-onConnect" target="_blank" rel="noreferrer">runtime.onConnect</a> at the other end.</li><li>The tab containing the port is unloaded (e.g. if the tab is navigated).</li><li>The frame where <code>connect()</code> was called has unloaded.</li><li>All frames that received the port (via <a href="https://developer.chrome.com/docs/extensions/reference/runtime#event-onConnect" target="_blank" rel="noreferrer">runtime.onConnect</a>) have unloaded.</li><li><a href="https://developer.chrome.com/docs/extensions/reference/runtime#property-Port-disconnect" target="_blank" rel="noreferrer">runtime.Port.disconnect()</a> is called by <em>the other end</em>. Note that if a <code>connect()</code> call results in multiple ports at the receiver&#39;s end, and <code>disconnect()</code> is called on any of these ports, then the <code>onDisconnect</code> event is only fired at the port of the sender, and not at the other ports.</li></ul><h2 id="cross-extension-messaging" tabindex="-1"><a href="https://developer.chrome.com/docs/extensions/mv3/messaging/#external" target="_blank" rel="noreferrer">#</a> Cross-extension messaging <a class="header-anchor" href="#cross-extension-messaging" aria-hidden="true">#</a></h2><p>In addition to sending messages between different components in your extension, you can use the messaging API to communicate with other extensions. This lets you expose a public API that other extensions can take advantage of.</p><p>Listening for incoming requests and connections is similar to the internal case, except you use the <a href="https://developer.chrome.com/docs/extensions/reference/runtime#event-onMessageExternal" target="_blank" rel="noreferrer">runtime.onMessageExternal</a> or <a href="https://developer.chrome.com/docs/extensions/reference/runtime#event-onConnectExternal" target="_blank" rel="noreferrer">runtime.onConnectExternal</a> methods. Here&#39;s an example of each:</p><pre><code>// For simple requests:chrome.runtime.onMessageExternal.addListener(  function(request, sender, sendResponse) {    if (sender.id === blocklistedExtension)      return;  // don&#39;t allow this extension access    else if (request.getTargetData)      sendResponse({targetData: targetData});    else if (request.activateLasers) {      var success = activateLasers();      sendResponse({activateLasers: success});    }  });// For long-lived connections:chrome.runtime.onConnectExternal.addListener(function(port) {  port.onMessage.addListener(function(msg) {    // See other examples for sample onMessage handlers.  });});
</code></pre><p>Likewise, sending a message to another extension is similar to sending one within your extension. The only difference is that you must pass the ID of the extension you want to communicate with. For example:</p><pre><code>// The ID of the extension we want to talk to.var laserExtensionId = &quot;abcdefghijklmnoabcdefhijklmnoabc&quot;;// Make a simple request:chrome.runtime.sendMessage(laserExtensionId, {getTargetData: true},  function(response) {    if (targetInRange(response.targetData))      chrome.runtime.sendMessage(laserExtensionId, {activateLasers: true});  });// Start a long-running conversation:var port = chrome.runtime.connect(laserExtensionId);port.postMessage(...);
</code></pre><h2 id="sending-messages-from-web-pages" tabindex="-1"><a href="https://developer.chrome.com/docs/extensions/mv3/messaging/#external-webpage" target="_blank" rel="noreferrer">#</a> Sending messages from web pages <a class="header-anchor" href="#sending-messages-from-web-pages" aria-hidden="true">#</a></h2><p>As with <a href="https://developer.chrome.com/docs/extensions/mv3/messaging/#external" target="_blank" rel="noreferrer">cross-extension messaging</a>, your extension can receive and respond to messages from regular web pages. To use this feature, you must first specify in your <code>manifest.json</code> which websites you want to communicate with using <a href="https://developer.chrome.com/docs/extensions/mv3/manifest/externally_connectable/" target="_blank" rel="noreferrer"><code>&quot;externally_connectable&quot;</code></a>. For example:</p><pre><code>&quot;externally_connectable&quot;: {  &quot;matches&quot;: [&quot;https://*.example.com/*&quot;]}
</code></pre><p>This exposes the messaging API to any page that matches the URL patterns you specify. The URL pattern must contain at least a <a href="https://en.wikipedia.org/wiki/Second-level_domain" target="_blank" rel="noreferrer">second-level domain</a>; that is, hostname patterns such as &quot;*&quot;, &quot;*.com&quot;, &quot;*.co.uk&quot;, and &quot;*.appspot.com&quot; are prohibited. From the web page, use the <a href="https://developer.chrome.com/docs/extensions/reference/runtime#method-sendMessage" target="_blank" rel="noreferrer">runtime.sendMessage()</a> or <a href="https://developer.chrome.com/docs/extensions/reference/runtime#method-connect" target="_blank" rel="noreferrer">runtime.connect()</a> APIs to send a message to a specific app or extension. For example:</p><pre><code>// The ID of the extension we want to talk to.var editorExtensionId = &quot;abcdefghijklmnoabcdefhijklmnoabc&quot;;// Make a simple request:chrome.runtime.sendMessage(editorExtensionId, {openUrlInEditor: url},  function(response) {    if (!response.success)      handleError(url);  });
</code></pre><p>From your extension, you may listen to messages from web pages via the <a href="https://developer.chrome.com/docs/extensions/reference/runtime#event-onMessageExternal" target="_blank" rel="noreferrer">runtime.onMessageExternal</a> or <a href="https://developer.chrome.com/docs/extensions/reference/runtime#event-onConnectExternal" target="_blank" rel="noreferrer">runtime.onConnectExternal</a> APIs, similar to <a href="https://developer.chrome.com/docs/extensions/mv3/messaging/#external" target="_blank" rel="noreferrer">cross-extension messaging</a>. Only the web page can initiate a connection. Here is an example:</p><pre><code>chrome.runtime.onMessageExternal.addListener(  function(request, sender, sendResponse) {    if (sender.url === blocklistedWebsite)      return;  // don&#39;t allow this web page access    if (request.openUrlInEditor)      openUrl(request.openUrlInEditor);  });
</code></pre><h2 id="native-messaging" tabindex="-1"><a href="https://developer.chrome.com/docs/extensions/mv3/messaging/#native-messaging" target="_blank" rel="noreferrer">#</a> Native messaging <a class="header-anchor" href="#native-messaging" aria-hidden="true">#</a></h2><p>Extensions <a href="https://developer.chrome.com/docs/apps/nativeMessaging/#native-messaging-client" target="_blank" rel="noreferrer">can exchange messages</a> with native applications that are registered as a <a href="https://developer.chrome.com/docs/apps/nativeMessaging/#native-messaging-host" target="_blank" rel="noreferrer">native messaging host</a>. To learn more about this feature, see <a href="https://developer.chrome.com/docs/apps/nativeMessaging/" target="_blank" rel="noreferrer">Native messaging</a>.</p><h2 id="security-considerations" tabindex="-1"><a href="https://developer.chrome.com/docs/extensions/mv3/messaging/#security-considerations" target="_blank" rel="noreferrer">#</a> Security considerations <a class="header-anchor" href="#security-considerations" aria-hidden="true">#</a></h2><h3 id="content-scripts-are-less-trustworthy" tabindex="-1"><a href="https://developer.chrome.com/docs/extensions/mv3/messaging/#content-scripts-are-less-trustworthy" target="_blank" rel="noreferrer">#</a> Content scripts are less trustworthy <a class="header-anchor" href="#content-scripts-are-less-trustworthy" aria-hidden="true">#</a></h3><p><a href="https://developer.chrome.com/docs/extensions/mv3/security#content_scripts" target="_blank" rel="noreferrer">Content scripts are less trustworthy</a> than the extension background page (e.g., a malicious web page might be able to compromise the renderer process where the content scripts run). Assume that messages from a content script might have been crafted by an attacker and make sure to <a href="https://developer.chrome.com/docs/extensions/mv3/security#sanitize" target="_blank" rel="noreferrer">validate and sanitize all input</a>. Assume any data sent to the content script might leak to the web page. Limit the scope of privileged actions that can be triggered by messages received from content scripts.</p><h3 id="cross-site-scripting" tabindex="-1"><a href="https://developer.chrome.com/docs/extensions/mv3/messaging/#cross-site-scripting" target="_blank" rel="noreferrer">#</a> Cross-site scripting <a class="header-anchor" href="#cross-site-scripting" aria-hidden="true">#</a></h3><p>When receiving a message from a content script or another extension, your scripts should be careful not to fall victim to <a href="https://en.wikipedia.org/wiki/Cross-site_scripting" target="_blank" rel="noreferrer">cross-site scripting</a>. This advice applies to scripts running inside the extension background page as well as to content scripts running inside other web origins. Specifically, avoid using dangerous APIs such as the ones below:</p><pre><code>chrome.tabs.sendMessage(tab.id, {greeting: &quot;hello&quot;}, function(response) {  // WARNING! Might be evaluating an evil script!  var resp = eval(&quot;(&quot; + response.farewell + &quot;)&quot;);});

chrome.tabs.sendMessage(tab.id, {greeting: &quot;hello&quot;}, function(response) {  // WARNING! Might be injecting a malicious script!  document.getElementById(&quot;resp&quot;).innerHTML = response.farewell;});
</code></pre><p>Instead, prefer safer APIs that do not run scripts:</p><pre><code>chrome.tabs.sendMessage(tab.id, {greeting: &quot;hello&quot;}, function(response) {  // JSON.parse does not evaluate the attacker&#39;s scripts.  var resp = JSON.parse(response.farewell);});

chrome.tabs.sendMessage(tab.id, {greeting: &quot;hello&quot;}, function(response) {  // innerText does not let the attacker inject HTML elements.  document.getElementById(&quot;resp&quot;).innerText = response.farewell;});
</code></pre><h2 id="examples" tabindex="-1"><a href="https://developer.chrome.com/docs/extensions/mv3/messaging/#examples" target="_blank" rel="noreferrer">#</a> Examples <a class="header-anchor" href="#examples" aria-hidden="true">#</a></h2><p>You can find simple examples of communication via messages in the <a href="https://github.com/GoogleChrome/chrome-extensions-samples/tree/master/mv2-archive/api/messaging" target="_blank" rel="noreferrer">examples/api/messaging</a> directory.</p><p>Updated on Friday, November 11, 2022 \u2022 <a href="https://github.com/GoogleChrome/developer.chrome.com/blob/main/site/en/docs/extensions/mv3/messaging/index.md" target="_blank" rel="noreferrer">Improve article</a></p>`,57),a=[o];function i(c,l,h,m,d,p){return t(),n("div",null,a)}const f=e(r,[["render",i]]);export{u as __pageData,f as default};
