import{_ as e,o as t,c as o,a as r}from"./app.8d96fb22.js";const f=JSON.parse('{"title":"Content scripts","description":"","frontmatter":{},"headers":[{"level":2,"title":"# Understand content script capabilities","slug":"understand-content-script-capabilities","link":"#understand-content-script-capabilities","children":[]},{"level":2,"title":"# Work in isolated worlds","slug":"work-in-isolated-worlds","link":"#work-in-isolated-worlds","children":[]},{"level":2,"title":"# Inject scripts","slug":"inject-scripts","link":"#inject-scripts","children":[{"level":3,"title":"# Inject with static declarations","slug":"inject-with-static-declarations","link":"#inject-with-static-declarations","children":[]},{"level":3,"title":"# Inject programmatically","slug":"inject-programmatically","link":"#inject-programmatically","children":[]},{"level":3,"title":"# Exclude matches and globs","slug":"exclude-matches-and-globs","link":"#exclude-matches-and-globs","children":[]},{"level":3,"title":"# Run time","slug":"run-time","link":"#run-time","children":[]},{"level":3,"title":"# Specify frames","slug":"specify-frames","link":"#specify-frames","children":[]},{"level":3,"title":"# Injecting in related frames","slug":"injecting-in-related-frames","link":"#injecting-in-related-frames","children":[]}]},{"level":2,"title":"# Communication with the embedding page","slug":"communication-with-the-embedding-page","link":"#communication-with-the-embedding-page","children":[]},{"level":2,"title":"# Stay secure","slug":"stay-secure","link":"#stay-secure","children":[]}],"relativePath":"chromedocs/In depth_ core concepts/Content scripts - Chrome Developers.md","lastUpdated":1679926139000}'),n={name:"chromedocs/In depth_ core concepts/Content scripts - Chrome Developers.md"},s=r(`<ul><li><a href="https://developer.chrome.com/docs/extensions/mv3/content_scripts/#capabilities" target="_blank" rel="noreferrer">Understand content script capabilities</a></li><li><a href="https://developer.chrome.com/docs/extensions/mv3/content_scripts/#isolated_world" target="_blank" rel="noreferrer">Work in isolated worlds</a></li><li><a href="https://developer.chrome.com/docs/extensions/mv3/content_scripts/#functionality" target="_blank" rel="noreferrer">Inject scripts</a><ul><li><a href="https://developer.chrome.com/docs/extensions/mv3/content_scripts/#static-declarative" target="_blank" rel="noreferrer">Inject with static declarations</a></li><li><a href="https://developer.chrome.com/docs/extensions/mv3/content_scripts/#programmatic" target="_blank" rel="noreferrer">Inject programmatically</a></li><li><a href="https://developer.chrome.com/docs/extensions/mv3/content_scripts/#matchAndGlob" target="_blank" rel="noreferrer">Exclude matches and globs</a></li><li><a href="https://developer.chrome.com/docs/extensions/mv3/content_scripts/#run_time" target="_blank" rel="noreferrer">Run time</a></li><li><a href="https://developer.chrome.com/docs/extensions/mv3/content_scripts/#frames" target="_blank" rel="noreferrer">Specify frames</a></li><li><a href="https://developer.chrome.com/docs/extensions/mv3/content_scripts/#injecting-in-related-frames" target="_blank" rel="noreferrer">Injecting in related frames</a></li></ul></li><li><a href="https://developer.chrome.com/docs/extensions/mv3/content_scripts/#host-page-communication" target="_blank" rel="noreferrer">Communication with the embedding page</a></li><li><a href="https://developer.chrome.com/docs/extensions/mv3/content_scripts/#security" target="_blank" rel="noreferrer">Stay secure</a></li></ul><h1 id="content-scripts" tabindex="-1">Content scripts <a class="header-anchor" href="#content-scripts" aria-hidden="true">#</a></h1><p>Published on Monday, September 17, 2012 \u2022 Updated on Monday, August 2, 2021</p><ul><li><a href="https://developer.chrome.com/docs/extensions/mv3/content_scripts/#capabilities" target="_blank" rel="noreferrer">Understand content script capabilities</a></li><li><a href="https://developer.chrome.com/docs/extensions/mv3/content_scripts/#isolated_world" target="_blank" rel="noreferrer">Work in isolated worlds</a></li><li><a href="https://developer.chrome.com/docs/extensions/mv3/content_scripts/#functionality" target="_blank" rel="noreferrer">Inject scripts</a><ul><li><a href="https://developer.chrome.com/docs/extensions/mv3/content_scripts/#static-declarative" target="_blank" rel="noreferrer">Inject with static declarations</a></li><li><a href="https://developer.chrome.com/docs/extensions/mv3/content_scripts/#programmatic" target="_blank" rel="noreferrer">Inject programmatically</a></li><li><a href="https://developer.chrome.com/docs/extensions/mv3/content_scripts/#matchAndGlob" target="_blank" rel="noreferrer">Exclude matches and globs</a></li><li><a href="https://developer.chrome.com/docs/extensions/mv3/content_scripts/#run_time" target="_blank" rel="noreferrer">Run time</a></li><li><a href="https://developer.chrome.com/docs/extensions/mv3/content_scripts/#frames" target="_blank" rel="noreferrer">Specify frames</a></li><li><a href="https://developer.chrome.com/docs/extensions/mv3/content_scripts/#injecting-in-related-frames" target="_blank" rel="noreferrer">Injecting in related frames</a></li></ul></li><li><a href="https://developer.chrome.com/docs/extensions/mv3/content_scripts/#host-page-communication" target="_blank" rel="noreferrer">Communication with the embedding page</a></li><li><a href="https://developer.chrome.com/docs/extensions/mv3/content_scripts/#security" target="_blank" rel="noreferrer">Stay secure</a></li></ul><p>Content scripts are files that run in the context of web pages. By using the standard <a href="https://www.w3.org/TR/DOM-Level-2-HTML/" target="_blank" rel="noreferrer">Document Object Model</a> (DOM), they are able to read details of the web pages the browser visits, make changes to them, and pass information to their parent extension.</p><h2 id="understand-content-script-capabilities" tabindex="-1"><a href="https://developer.chrome.com/docs/extensions/mv3/content_scripts/#capabilities" target="_blank" rel="noreferrer">#</a> Understand content script capabilities <a class="header-anchor" href="#understand-content-script-capabilities" aria-hidden="true">#</a></h2><p>Content scripts can access Chrome APIs used by their parent extension by exchanging <a href="https://developer.chrome.com/docs/extensions/mv3/messaging/" target="_blank" rel="noreferrer">messages</a> with the extension. They can also access the URL of an extension&#39;s file with <code>chrome.runtime.getURL()</code> and use the result the same as other URLs.</p><pre><code>// Code for displaying EXTENSION_DIR/images/myimage.png:var imgURL = chrome.runtime.getURL(&quot;images/myimage.png&quot;);document.getElementById(&quot;someImage&quot;).src = imgURL;
</code></pre><p>Additionally, content scripts can access the following chrome APIs directly:</p><ul><li><a href="https://developer.chrome.com/docs/extensions/reference/i18n/" target="_blank" rel="noreferrer">i18n</a></li><li><a href="https://developer.chrome.com/docs/extensions/reference/storage/" target="_blank" rel="noreferrer">storage</a></li><li><a href="https://developer.chrome.com/docs/extensions/reference/runtime/" target="_blank" rel="noreferrer">runtime</a>: <ul><li><a href="https://developer.chrome.com/docs/extensions/reference/runtime#method-connect" target="_blank" rel="noreferrer">connect</a></li><li><a href="https://developer.chrome.com/docs/extensions/reference/runtime#method-getManifest" target="_blank" rel="noreferrer">getManifest</a></li><li><a href="https://developer.chrome.com/docs/extensions/reference/runtime#method-getURL" target="_blank" rel="noreferrer">getURL</a></li><li><a href="https://developer.chrome.com/docs/extensions/reference/runtime#property-id" target="_blank" rel="noreferrer">id</a></li><li><a href="https://developer.chrome.com/docs/extensions/reference/runtime#event-onConnect" target="_blank" rel="noreferrer">onConnect</a></li><li><a href="https://developer.chrome.com/docs/extensions/reference/runtime#event-onMessage" target="_blank" rel="noreferrer">onMessage</a></li><li><a href="https://developer.chrome.com/docs/extensions/reference/runtime#method-sendMessage" target="_blank" rel="noreferrer">sendMessage</a></li></ul></li></ul><p>Content scripts are unable to access other APIs directly.</p><h2 id="work-in-isolated-worlds" tabindex="-1"><a href="https://developer.chrome.com/docs/extensions/mv3/content_scripts/#isolated_world" target="_blank" rel="noreferrer">#</a> Work in isolated worlds <a class="header-anchor" href="#work-in-isolated-worlds" aria-hidden="true">#</a></h2><p>Content scripts live in an isolated world, allowing a content script to make changes to its JavaScript environment without conflicting with the page or other extensions&#39; content scripts.</p><p>An <em>isolated world</em> is a private execution environment that isn&#39;t accessible to the page or other extensions. A practical consequence of this isolation is that JavaScript variables in an extension&#39;s content scripts are not visible to the host page or other extensions&#39; content scripts. The concept was originally introduced with the initial launch of Chrome, providing isolation for browser tabs.</p><p>An extension may run in a web page with code similar to the example below.</p><pre><code>&lt;html&gt;  &lt;button id=&quot;mybutton&quot;&gt;click me&lt;/button&gt;  &lt;script&gt;    var greeting = &quot;hello, &quot;;    var button = document.getElementById(&quot;mybutton&quot;);    button.person_name = &quot;Bob&quot;;    button.addEventListener(&quot;click&quot;, () =&gt;      alert(greeting + button.person_name + &quot;.&quot;)    , false);  &lt;/script&gt;&lt;/html&gt;
</code></pre><p>That extension could inject the following content script using one of the techniques outlined in the <a href="https://developer.chrome.com/docs/extensions/mv3/content_scripts/#functionality" target="_blank" rel="noreferrer">Inject scripts</a> section.</p><pre><code>var greeting = &quot;hola, &quot;;var button = document.getElementById(&quot;mybutton&quot;);button.person_name = &quot;Roberto&quot;;button.addEventListener(&quot;click&quot;, () =&gt;  alert(greeting + button.person_name + &quot;.&quot;), false);
</code></pre><p>With this change, both alerts appear in sequence when the button is clicked.</p><p>Not only does each extension run in its own isolated world, but content scripts and the web page do too. This means that none of these (web page, content scripts, and any running extensions) can access the context and variables of the others.</p><h2 id="inject-scripts" tabindex="-1"><a href="https://developer.chrome.com/docs/extensions/mv3/content_scripts/#functionality" target="_blank" rel="noreferrer">#</a> Inject scripts <a class="header-anchor" href="#inject-scripts" aria-hidden="true">#</a></h2><p>Content scripts can be <a href="https://developer.chrome.com/docs/extensions/mv3/content_scripts/#static-declarative" target="_blank" rel="noreferrer">declared statically</a> or <a href="https://developer.chrome.com/docs/extensions/mv3/content_scripts/#programmatic" target="_blank" rel="noreferrer">programmatically injected</a>.</p><h3 id="inject-with-static-declarations" tabindex="-1"><a href="https://developer.chrome.com/docs/extensions/mv3/content_scripts/#static-declarative" target="_blank" rel="noreferrer">#</a> Inject with static declarations <a class="header-anchor" href="#inject-with-static-declarations" aria-hidden="true">#</a></h3><p>Use static content script declarations in manifest.json for scripts that should be automatically run on a well known set of pages.</p><p>Statically declared scripts are registered in the manifest under the <code>&quot;content_scripts&quot;</code> field. They can include JavaScript files, CSS files, or both. All auto-run content scripts must specify <a href="https://developer.chrome.com/docs/extensions/mv3/match_patterns/" target="_blank" rel="noreferrer">match patterns</a>.</p><pre><code>{ &quot;name&quot;: &quot;My extension&quot;, ... &quot;content_scripts&quot;: [   {     &quot;matches&quot;: [&quot;https://*.nytimes.com/*&quot;],     &quot;css&quot;: [&quot;my-styles.css&quot;],     &quot;js&quot;: [&quot;content-script.js&quot;]   } ], ...}
</code></pre><p>Name</p><p>Type</p><p>Description</p><p><code>matches</code></p><p>array of strings</p><p><em>Required.</em> Specifies which pages this content script will be injected into. See <a href="https://developer.chrome.com/docs/extensions/mv3/match_patterns/" target="_blank" rel="noreferrer">Match Patterns</a> for more details on the syntax of these strings and <a href="https://developer.chrome.com/docs/extensions/mv3/content_scripts/#matchAndGlob" target="_blank" rel="noreferrer">Match patterns and globs</a> for information on how to exclude URLs.</p><p><code>css</code></p><p>array of strings</p><p><em>Optional.</em> The list of CSS files to be injected into matching pages. These are injected in the order they appear in this array, before any DOM is constructed or displayed for the page.</p><p><code>js</code></p><p>array of strings</p><p><em>Optional.</em> The list of JavaScript files to be injected into matching pages. These are injected in the order they appear in this array.</p><p><code>match_about_blank</code></p><p>boolean</p><p><em>Optional.</em> Whether the script should inject into an <code>about:blank</code> frame where the parent or opener frame matches one of the patterns declared in <code>matches</code>. Defaults to false.</p><p><code>match_origin_as_fallback</code></p><p>boolean</p><p><em>Optional.</em> Whether the script should inject in frames that were created by a matching origin, but whose URL or origin may not directly match the pattern. These include frames with different schemes, such as <code>about:</code>, <code>data:</code>, <code>blob:</code>, and <code>filesystem:</code>. See also <a href="https://developer.chrome.com/docs/extensions/mv3/content_scripts/#injecting-in-related-frames" target="_blank" rel="noreferrer">Injecting in related frames</a>.</p><h3 id="inject-programmatically" tabindex="-1"><a href="https://developer.chrome.com/docs/extensions/mv3/content_scripts/#programmatic" target="_blank" rel="noreferrer">#</a> Inject programmatically <a class="header-anchor" href="#inject-programmatically" aria-hidden="true">#</a></h3><p>Use programmatic injection for content scripts that need to run in response to events or on specific occasions.</p><p>To inject a content script programmatically, your extension needs host permissions for the page it&#39;s trying to inject scripts into. Host permissions can either be granted by requesting them as part of your extension&#39;s manifest (see <a href="https://developer.chrome.com/docs/extensions/reference/permissions/" target="_blank" rel="noreferrer"><code>host_permissions</code></a>) or temporarily via <a href="https://developer.chrome.com/activeTab/" target="_blank" rel="noreferrer">activeTab</a>.</p><p>Below we&#39;ll look at different versions of an activeTab-based extension.</p><p>manifest.json:</p><pre><code>{  &quot;name&quot;: &quot;My extension&quot;,  ...  &quot;permissions&quot;: [    &quot;activeTab&quot;  ],  &quot;background&quot;: {    &quot;service_worker&quot;: &quot;background.js&quot;  }}
</code></pre><p>Content scripts can be injected as files\u2026</p><p>content-script.js:</p><pre><code>document.body.style.backgroundColor = &#39;orange&#39;;
</code></pre><p>background.js:</p><pre><code>chrome.action.onClicked.addListener((tab) =&gt; {  chrome.scripting.executeScript({    target: { tabId: tab.id },    files: [&#39;content-script.js&#39;]  });});
</code></pre><p>\u2026or a function body can be injected and executed as a content script.</p><p>background.js:</p><pre><code>function injectedFunction() {  document.body.style.backgroundColor = &#39;orange&#39;;}chrome.action.onClicked.addListener((tab) =&gt; {  chrome.scripting.executeScript({    target: { tabId: tab.id },    func: injectedFunction  });});
</code></pre><p>Be aware that the injected function is a copy of the function referenced in the <code>chrome.scripting.executeScript</code> call, not the original function itself. As a result, the function&#39;s body must be self contained; references to variables outside of the function will cause the content script to throw a <a href="https://developer.mozilla.org/docs/Web/JavaScript/Reference/Global_Objects/ReferenceError" target="_blank" rel="noreferrer"><code>ReferenceError</code></a>.</p><h3 id="exclude-matches-and-globs" tabindex="-1"><a href="https://developer.chrome.com/docs/extensions/mv3/content_scripts/#matchAndGlob" target="_blank" rel="noreferrer">#</a> Exclude matches and globs <a class="header-anchor" href="#exclude-matches-and-globs" aria-hidden="true">#</a></h3><p>Specified page matching is customizable by including the following fields in a declarative registration.</p><p>Name</p><p>Type</p><p>Description</p><p><code>exclude_matches</code></p><p>array of strings</p><p><em>Optional.</em> Excludes pages that this content script would otherwise be injected into. See <a href="https://developer.chrome.com/docs/extensions/mv3/content_scripts/match_patterns" target="_blank" rel="noreferrer">Match Patterns</a> for more details on the syntax of these strings.</p><p><code>include_globs</code></p><p>array of strings</p><p><em>Optional.</em> Applied after <code>matches</code> to include only those URLs that also match this glob. Intended to emulate the <a href="https://wiki.greasespot.net/Metadata_Block#.40include" target="_blank" rel="noreferrer"><code>@include</code></a> Greasemonkey keyword.</p><p><code>exclude_globs</code></p><p>array of string</p><p><em>Optional.</em> Applied after <code>matches</code> to exclude URLs that match this glob. Intended to emulate the <a href="https://wiki.greasespot.net/Metadata_Block#.40exclude" target="_blank" rel="noreferrer"><code>@exclude</code></a> Greasemonkey keyword.</p><p>The content script will be injected into a page if both of the following are true:</p><ul><li>Its URL matches any <code>matches</code> pattern and any <code>include_globs</code> pattern</li><li>The URL doesn&#39;t also match an <code>exclude_matches</code> or <code>exclude_globs</code> pattern. Because the <code>matches</code> property is required, <code>exclude_matches</code>, <code>include_globs</code>, and <code>exclude_globs</code> can only be used to limit which pages will be affected.</li></ul><p>The following extension injects the content script into <strong><a href="https://www.nytimes.com/" target="_blank" rel="noreferrer">https://www.nytimes.com/</a> health</strong> but not into <strong><a href="https://www.nytimes.com/" target="_blank" rel="noreferrer">https://www.nytimes.com/</a> business</strong> .</p><pre><code>{  &quot;name&quot;: &quot;My extension&quot;,  ...  &quot;content_scripts&quot;: [    {      &quot;matches&quot;: [&quot;https://*.nytimes.com/*&quot;],      &quot;exclude_matches&quot;: [&quot;*://*/*business*&quot;],      &quot;js&quot;: [&quot;contentScript.js&quot;]    }  ],  ...}

chrome.scripting.registerContentScript({  id: 1,  matches: [&quot;https://*.nytimes.com/*&quot;],  exclude_matches: [&quot;*://*/*business*&quot;],  js: [&quot;contentScript.js&quot;]});
</code></pre><p>Glob properties follow a different, more flexible syntax than <a href="https://developer.chrome.com/docs/extensions/mv3/match_patterns/" target="_blank" rel="noreferrer">match patterns</a>. Acceptable glob strings are URLs that may contain &quot;wildcard&quot; asterisks and question marks. The asterisk <strong>*</strong> matches any string of any length, including the empty string, while the question mark <strong>?</strong> matches any single character.</p><p>For example, the glob <strong>https://???.example.com/foo/*</strong> matches any of the following:</p><ul><li><strong><a href="https://www.example.com/foo/bar" target="_blank" rel="noreferrer">https://www.example.com/foo/bar</a></strong></li><li><strong><a href="https://the.example.com/foo/" target="_blank" rel="noreferrer">https://the.example.com/foo/</a></strong></li></ul><p>However, it does <em>not</em> match the following:</p><ul><li><strong><a href="https://my.example.com/foo/bar" target="_blank" rel="noreferrer">https://my.example.com/foo/bar</a></strong></li><li><strong><a href="https://example.com/foo/" target="_blank" rel="noreferrer">https://example.com/foo/</a></strong></li><li><strong><a href="https://www.example.com/foo" target="_blank" rel="noreferrer">https://www.example.com/foo</a></strong></li></ul><p>This extension injects the content script into <strong><a href="https://www.nytimes.com/arts/index.html" target="_blank" rel="noreferrer">https://www.nytimes.com/arts/index.html</a></strong> and <strong><a href="https://www.nytimes.com/jobs/index.html" target="_blank" rel="noreferrer">https://www.nytimes.com/jobs/index.html</a></strong>, but not into **<a href="https://www.nytimes.com/sports/index.html**:" target="_blank" rel="noreferrer">https://www.nytimes.com/sports/index.html**:</a></p><pre><code>{  &quot;name&quot;: &quot;My extension&quot;,  ...  &quot;content_scripts&quot;: [    {      &quot;matches&quot;: [&quot;https://*.nytimes.com/*&quot;],      &quot;include_globs&quot;: [&quot;*nytimes.com/???s/*&quot;],      &quot;js&quot;: [&quot;contentScript.js&quot;]    }  ],  ...}

chrome.scripting.registerContentScript({  id: 1,  matches: [&#39;https://*.nytimes.com/*&#39;],  include_globs: [&#39;*nytimes.com/???s/*&#39;],  js: [&#39;contentScript.js&#39;]});
</code></pre><p>This extension injects the content script into <strong><a href="https://history.nytimes.com" target="_blank" rel="noreferrer">https://history.nytimes.com</a></strong> and <strong>https://.nytimes.com/history</strong>, but not into <strong><a href="https://science.nytimes.com" target="_blank" rel="noreferrer">https://science.nytimes.com</a></strong> or **<a href="https://www.nytimes.com/science**:" target="_blank" rel="noreferrer">https://www.nytimes.com/science**:</a></p><pre><code>{  &quot;name&quot;: &quot;My extension&quot;,  ...  &quot;content_scripts&quot;: [    {      &quot;matches&quot;: [&quot;https://*.nytimes.com/*&quot;],      &quot;exclude_globs&quot;: [&quot;*science*&quot;],      &quot;js&quot;: [&quot;contentScript.js&quot;]    }  ],  ...}

chrome.scripting.registerContentScript({  id: 1,  matches: [&#39;https://*.nytimes.com/*&#39;],  exclude_globs: [&#39;*science*&#39;],  js: [&#39;contentScript.js&#39;]});
</code></pre><p>One, all, or some of these can be included to achieve the correct scope.</p><pre><code>{  &quot;name&quot;: &quot;My extension&quot;,  ...  &quot;content_scripts&quot;: [    {      &quot;matches&quot;: [&quot;https://*.nytimes.com/*&quot;],      &quot;exclude_matches&quot;: [&quot;*://*/*business*&quot;],      &quot;include_globs&quot;: [&quot;*nytimes.com/???s/*&quot;],      &quot;exclude_globs&quot;: [&quot;*science*&quot;],      &quot;js&quot;: [&quot;contentScript.js&quot;]    }  ],  ...}

chrome.scripting.registerContentScript({  matches: [&#39;https://*.nytimes.com/*&#39;],  exclude_matches: [&#39;*://*/*business*&#39;],  include_globs: [&#39;*nytimes.com/???s/*&#39;],  exclude_globs: [&#39;*science*&#39;],  js: [&#39;contentScript.js&#39;]});
</code></pre><h3 id="run-time" tabindex="-1"><a href="https://developer.chrome.com/docs/extensions/mv3/content_scripts/#run_time" target="_blank" rel="noreferrer">#</a> Run time <a class="header-anchor" href="#run-time" aria-hidden="true">#</a></h3><p>The <code>run_at</code> field controls when JavaScript files are injected into the web page. The preferred and default value is <code>&quot;document_idle&quot;</code>, but you can also specify <code>&quot;document_start&quot;</code> or <code>&quot;document_end&quot;</code> if needed.</p><pre><code>{  &quot;name&quot;: &quot;My extension&quot;,  ...  &quot;content_scripts&quot;: [    {      &quot;matches&quot;: [&quot;https://*.nytimes.com/*&quot;],      &quot;run_at&quot;: &quot;document_idle&quot;,      &quot;js&quot;: [&quot;contentScript.js&quot;]    }  ],  ...}

chrome.scripting.registerContentScript({  matches: [&#39;https://*.nytimes.com/*&#39;],  run_at: &#39;document_idle&#39;,  js: [&#39;contentScript.js&#39;]});
</code></pre><p>Name</p><p>Type</p><p>Description</p><p><code>document_idle</code></p><p>string</p><p><em>Preferred.</em> Use <code>&quot;document_idle&quot;</code> whenever possible.</p><p>The browser chooses a time to inject scripts between <code>&quot;document_end&quot;</code> and immediately after the <a href="https://www.whatwg.org/specs/web-apps/current-work/#handler-onload" target="_blank" rel="noreferrer"><code>window.onload</code></a> event fires. The exact moment of injection depends on how complex the document is and how long it is taking to load, and is optimized for page load speed.</p><p>Content scripts running at <code>&quot;document_idle&quot;</code> do not need to listen for the <code>window.onload</code> event, they are guaranteed to run after the DOM is complete. If a script definitely needs to run after <code>window.onload</code>, the extension can check if <code>onload</code> has already fired by using the <a href="https://www.whatwg.org/specs/web-apps/current-work/#dom-document-readystate" target="_blank" rel="noreferrer"><code>document.readyState</code></a> property.</p><p><code>document_start</code></p><p>string</p><p>Scripts are injected after any files from <code>css</code>, but before any other DOM is constructed or any other script is run.</p><p><code>document_end</code></p><p>string</p><p>Scripts are injected immediately after the DOM is complete, but before subresources like images and frames have loaded.</p><h3 id="specify-frames" tabindex="-1"><a href="https://developer.chrome.com/docs/extensions/mv3/content_scripts/#frames" target="_blank" rel="noreferrer">#</a> Specify frames <a class="header-anchor" href="#specify-frames" aria-hidden="true">#</a></h3><p>The <code>&quot;all_frames&quot;</code> field allows the extension to specify if JavaScript and CSS files should be injected into all frames matching the specified URL requirements or only into the topmost frame in a tab.</p><pre><code>{  &quot;name&quot;: &quot;My extension&quot;,  ...  &quot;content_scripts&quot;: [    {      &quot;matches&quot;: [&quot;https://*.nytimes.com/*&quot;],      &quot;all_frames&quot;: true,      &quot;js&quot;: [&quot;contentScript.js&quot;]    }  ],  ...}

chrome.scripting.registerContentScript({  matches: [&#39;https://*.nytimes.com/*&#39;],  all_frames: true,  js: [&#39;contentScript.js&#39;]});
</code></pre><p>Name</p><p>Type</p><p>Description</p><p><code>all_frames</code></p><p>boolean</p><p><em>Optional.</em> Defaults to <code>false</code>, meaning that only the top frame is matched.</p><p>If specified <code>true</code>, it will inject into all frames, even if the frame is not the topmost frame in the tab. Each frame is checked independently for URL requirements, it won&#39;t inject into child frames if the URL requirements are not met.</p><h3 id="injecting-in-related-frames" tabindex="-1"><a href="https://developer.chrome.com/docs/extensions/mv3/content_scripts/#injecting-in-related-frames" target="_blank" rel="noreferrer">#</a> Injecting in related frames <a class="header-anchor" href="#injecting-in-related-frames" aria-hidden="true">#</a></h3><p>Extensions may want to run scripts in frames that are related to a matching frame, but don&#39;t themselves match. A common scenario when this is the case is for frames with URLs that were created by a matching frame, but whose URLs don&#39;t themselves match the script&#39;s specified patterns.</p><p>This is the case when an extension wants to inject in frames with URLs that have <code>about:</code>, <code>data:</code>, <code>blob:</code>, and <code>filesystem:</code> schemes. In these cases, the URL will not match the content script&#39;s pattern (and, in the case of <code>about:</code> and <code>data:</code>, do not even include the parent URL or origin in the URL at all, as in <code>about:blank</code> or <code>data:text/html,&lt;html&gt;Hello, World!&lt;/html&gt;</code>). However, these frames can still be associated with the creating frame.</p><p>To inject into these frames, extensions can specify the <code>&quot;match_origin_as_fallback&quot;</code> property on a content script specification in the manifest.</p><pre><code>{  &quot;name&quot;: &quot;My extension&quot;,  ...  &quot;content_scripts&quot;: [    {      &quot;matches&quot;: [&quot;https://*.google.com/*&quot;],      &quot;match_origin_as_fallback&quot;: true,      &quot;js&quot;: [&quot;contentScript.js&quot;]    }  ],  ...}
</code></pre><p>When specified and set to <code>true</code>, Chrome will look at the origin of the initiator of the frame to determine whether the frame matches, rather than at the URL of the frame itself. Note that this might also be different than the target frame&#39;s <em>origin</em> (e.g., <code>data:</code> URLs have a null origin).</p><p>The initiator of the frame is the frame that created or navigated the target frame. While this is commonly the direct parent or opener, it may not be (as in the case of a frame navigating an iframe within an iframe).</p><p>Because this compares the <em>origin</em> of the initiator frame, the initiator frame could be on at any path from that origin. To make this implication clear, Chrome requires any content scripts specified with <code>&quot;match_origin_as_fallback&quot;</code> set to <code>true</code> to also specify a path of <code>*</code>.</p><p>When both <code>&quot;match_origin_as_fallback&quot;</code> and <code>&quot;match_about_blank&quot;</code> are specified, <code>&quot;match_origin_as_fallback&quot;</code> takes priority.</p><p>This property is only available in extensions running manifest version 3 or higher.</p><h2 id="communication-with-the-embedding-page" tabindex="-1"><a href="https://developer.chrome.com/docs/extensions/mv3/content_scripts/#host-page-communication" target="_blank" rel="noreferrer">#</a> Communication with the embedding page <a class="header-anchor" href="#communication-with-the-embedding-page" aria-hidden="true">#</a></h2><p>Although the execution environments of content scripts and the pages that host them are isolated from each other, they share access to the page&#39;s DOM. If the page wishes to communicate with the content script, or with the extension via the content script, it must do so through the shared DOM.</p><p>An example can be accomplished using <a href="https://developer.mozilla.org/docs/Web/API/Window/postMessage" target="_blank" rel="noreferrer"><code>window.postMessage</code></a>:</p><pre><code>var port = chrome.runtime.connect();window.addEventListener(&quot;message&quot;, (event) =&gt; {  // We only accept messages from ourselves  if (event.source != window) {    return;  }  if (event.data.type &amp;&amp; (event.data.type == &quot;FROM_PAGE&quot;)) {    console.log(&quot;Content script received: &quot; + event.data.text);    port.postMessage(event.data.text);  }}, false);

document.getElementById(&quot;theButton&quot;).addEventListener(&quot;click&quot;, () =&gt; {  window.postMessage({ type: &quot;FROM_PAGE&quot;, text: &quot;Hello from the webpage!&quot; }, &quot;*&quot;);}, false);
</code></pre><p>The non-extension page, example.html, posts messages to itself. This message is intercepted and inspected by the content script and then posted to the extension process. In this way, the page establishes a line of communication to the extension process. The reverse is possible through similar means.</p><h2 id="stay-secure" tabindex="-1"><a href="https://developer.chrome.com/docs/extensions/mv3/content_scripts/#security" target="_blank" rel="noreferrer">#</a> Stay secure <a class="header-anchor" href="#stay-secure" aria-hidden="true">#</a></h2><p>While isolated worlds provide a layer of protection, using content scripts can create vulnerabilities in an extension and the web page. If the content script receives content from a separate website, such as making an <a href="https://developer.chrome.com/docs/extensions/mv3/xhr/" target="_blank" rel="noreferrer"><code>XMLHttpRequest</code></a>, be careful to filter content <a href="https://en.wikipedia.org/wiki/Cross-site_scripting" target="_blank" rel="noreferrer">cross-site scripting</a> attacks before injecting it. Only communicate over HTTPS in order to avoid <a href="https://en.wikipedia.org/wiki/Man-in-the-middle_attack" target="_blank" rel="noreferrer">&quot;man-in-the-middle&quot;</a> attacks.</p><p>Be sure to filter for malicious web pages. For example, the following patterns are dangerous, and disallowed in Manifest V3:</p><p>Don&#39;t</p><pre><code>const data = document.getElementById(&quot;json-data&quot;)// WARNING! Might be evaluating an evil script!const parsed = eval(&quot;(&quot; + data + &quot;)&quot;)
</code></pre><p>Don&#39;t</p><pre><code>const elmt_id = ...// WARNING! elmt_id might be &quot;); ... evil script ... //&quot;!window.setTimeout(&quot;animate(&quot; + elmt_id + &quot;)&quot;, 200);
</code></pre><p>Instead, prefer safer APIs that do not run scripts:</p><p>Do</p><pre><code>const data = document.getElementById(&quot;json-data&quot;)// JSON.parse does not evaluate the attacker&#39;s scripts.const parsed = JSON.parse(data);
</code></pre><p>Do</p><pre><code>const elmt_id = ...// The closure form of setTimeout does not evaluate scripts.window.setTimeout(() =&gt; animate(elmt_id), 200);
</code></pre><p>Updated on Monday, August 2, 2021 \u2022 <a href="https://github.com/GoogleChrome/developer.chrome.com/blob/main/site/en/docs/extensions/mv3/content_scripts/index.md" target="_blank" rel="noreferrer">Improve article</a></p>`,143),a=[s];function i(c,l,d,p,h,m){return t(),o("div",null,a)}const g=e(n,[["render",i]]);export{f as __pageData,g as default};
